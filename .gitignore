# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ── Signing secrets — never commit ──
# The release key cannot be rotated: losing it blocks all future updates, and
# leaking it lets anyone publish builds that look like yours.
*.jks
*.keystore
android/keystore.properties
!android/keystore.properties.example

# Android build output / machine-specific paths
android/build
android/app/build
android/capacitor-cordova-android-plugins/build
android/.gradle
android/local.properties
*.apk
build-output

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
